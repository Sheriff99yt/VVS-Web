/**
 * Stage C — IR + language profile → source strings.
 * Per-language emitters live alongside generate.ts until the IR split is complete.
 */
export { generateMockCode, generateMockTranspileResult } from '../generate';
