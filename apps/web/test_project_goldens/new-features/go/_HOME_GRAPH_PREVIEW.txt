// ===== src/NewFeaturesLab.go =====
import "fmt"
type MainClass struct {
	var GlobalGreeting string = "World"
func ProcessData(a, b) data_number {
	return (a + b)
}
	// (x) Implement ProcessData
func (self *MainClass) on_ondatareceived(payload string) {
	fmt.Println(self.ProcessData(payload, payload))
}
func (self *MainClass) on_start() {
	self.on_ondatareceived(("Hello from Global: " + self.GlobalGreeting))
}
}