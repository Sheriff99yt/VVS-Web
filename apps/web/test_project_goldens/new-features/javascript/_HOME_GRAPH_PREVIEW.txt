// ===== src/NewFeaturesLab.js =====
class MainClass {
    GlobalGreeting = "World";
    ProcessData(a, b) {
        return (a + b);
    }
    ProcessData(a, b) {
        return (a + b);
        // Concat Strings
    }
    on_ondatareceived(payload) {
        this.ProcessData(payload, payload);
        console.log("/* Call ProcessData.return_val */");
    }
    on_start() {
        this.on_ondatareceived(("Hello from Global: " + this.GlobalGreeting));
    }
}