// ===== src/Counter.js =====
const Mode = Object.freeze({
  Idle: 1,
  Run: 2
});
class Counter {
    Total = 0;
    Limit = 3;
    Ready = true;
    CurrentMode = Mode.Run;
    Add(n) {
        this.Total = (this.Total + n);
        return this.Total;
    }
    on_start() {
        if (this.Ready) {
            console.log("go");
            for (let _vvs_i_cx_for_limit = 1; _vvs_i_cx_for_limit <= this.Limit; _vvs_i_cx_for_limit++) {
                this.Add(1);
                console.log(String(this.Total));
            };
            switch (this.CurrentMode) {
              case Mode.Idle:
                console.log("Idle");
                    break;
              case Mode.Run:
                console.log("Run");
                    break;
            };
        } else {
            console.log("skip");
        };
    }
}