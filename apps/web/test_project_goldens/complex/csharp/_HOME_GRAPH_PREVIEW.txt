// ===== src/Counter.cs =====
public enum Mode {
    Idle,
    Run
}
public class Counter {
    public float Total = 0;
    public float Limit = 3;
    public bool Ready = true;
    public Mode CurrentMode = Mode.Run;
    public float Add(float n) {
        this.Total = (this.Total + n);
        return this.Total;
    }
    public void on_start() {
        if (this.Ready) {
            Console.WriteLine("go");
            for (int _vvs_i_cx_for_limit = 1; _vvs_i_cx_for_limit <= this.Limit; _vvs_i_cx_for_limit++) {
                this.Add(1);
                Console.WriteLine(this.Total.ToString());
            }
            switch (this.CurrentMode) {
                case Mode.Idle:
                Console.WriteLine("Idle");
                        break;
                case Mode.Run:
                Console.WriteLine("Run");
                        break;
            }
        } else {
            Console.WriteLine("skip");
        }
    }
}