# Test Project generated code (historical extracts)

**Superseded as product examples.** Start-screen cards today are **Simple** (`vvs-test-simple`), **Complex** (`vvs-test-complex`), and **Advanced** (`vvs-test-advanced`). Home-preview goldens (U65) use those names.

This folder is a leftover on-disk extract from **retired labs**. Filenames still use the old class names. Do not treat these as shipped Start cards. Do not mix them with **Rosetta** pack fixtures (`packages/syntax-packs/rosetta/`).

Extracted via disk seed → load → `emitProjectLikeCodePanel` — **one graph → one file**.

## First Graph (`simple`) — retired lab extract

On-disk files still named `src__FirstGraph.*` under `simple/<lang>/`.

## Branch Lab (`branch`) — retired lab extract

On-disk files still named `src__BranchLab.*` under `branch/<lang>/`.

## Coverage Lab (`complex`) — retired lab extract

On-disk files still named `src__CoverageLab.*` under `complex/<lang>/`. This folder is **not** the shipped Complex home-preview golden.

## New Features Lab (`new-features`) — retired lab extract

On-disk files still named `src__NewFeaturesLab.*` under `new-features/<lang>/`.

Also retired (not in this folder): Inheritance, Calculator, Dual Class, Async Fetcher.
